using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class public_variables : MonoBehaviour
{
    public int score;
    public bool stealer;
    // Start is called before the first frame update
    void Start()
    {
        score = 0;
        stealer = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
